<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Dark theme for your login page -->
    <style name="DarkLoginTheme" parent="Theme.AppCompat">
        <item name="android:windowBackground">@color/darkBackground</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="colorPrimary">@color/darkPrimary</item>
        <item name="colorPrimaryDark">@color/darkPrimaryDark</item>
        <item name="colorAccent">@color/darkAccent</item>
        <!-- Add more custom attributes as needed -->
    </style>


    <!-- Style for email input -->
    <style name="EmailEditTextStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:paddingStart">16dp</item>
        <item name="android:paddingEnd">16dp</item>
        <item name="android:layout_marginTop">8dp</item>
        <item name="android:hint">Email</item>
        <item name="android:textSize">20sp</item>
        <item name="android:inputType">textEmailAddress</item>
    </style>

    <!-- Style for password input -->
    <style name="PasswordEditTextStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:paddingStart">16dp</item>
        <item name="android:paddingEnd">16dp</item>
        <item name="android:layout_marginTop">8dp</item>
        <item name="android:hint">Password</item>
        <item name="android:textSize">20sp</item>
        <item name="android:inputType">textPassword</item>
    </style>

    <color name="colorPrimaryDark">#242424</color>
    <color name="colorAccent">#40303F</color>
</resources>
